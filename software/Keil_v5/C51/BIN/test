#include<reg51.h>
sbit LED1=P2^0;
void delay(unsigned int i);

void main(void)
{
	while(1)
	{
	  LED1=0;				//LED ON
	  delay(150);
	  LED1=1;				//LED OFF
	  delay(150);
	}
}

void delay(unsigned int i)
	{
		unsigned int k;
	
		for(k=0;k<i;k++)
		for(k=0;k<1500;k++);
	}
		
		
